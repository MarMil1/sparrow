<nav class="border-gray-200 mb-6">
    <div class="flex flex-wrap items-center justify-between mx-auto p-4">
        <div class="flex items-center justify-between gap-2">
            <div class="mr-8">
                <%= link_to "Sparrow", root_path %>
            </div>
            <div class="text-gray-500 hover:underline underline-offset-4 decoration-gray-500">
                <% if current_user %>
                    <% if current_user.roles.first.name == "admin" %>
                        <%= link_to "Users", users_path %>
                    <% end %> 
                <% end %> 
            </div>
        </div>
        <div>
            <% if current_user %>
                <div class="flex items-center justify-between gap-2">
                    <div>
                        <%= current_user.email %>
                    </div>

                    <div>
                        Role: <%= current_user.roles.pluck(:name).join(', ') %>
                    </div>

                    <div>
                        <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "bg-rose-400 p-2 rounded-lg text-white hover:bg-rose-300" %>
                    </div>
                    <div class="dropdown relative text-sm" data-dropdown>
                        <button class="dropbtn rounded-full border border-2 border-blue-500 bg-blue-300 font-semibold text-[#101827]" data-dropdown-toggle>
                            <div class="border border-2 border-white p-2 rounded-full">
                                AE
                            </div>
                        </button>
                        <div class="dropdown-content hidden absolute bg-white mt-2 right-0 rounded-lg shadow">
                            <div class="border-b p-3 flex justify-center items-center gap-6">
                                <div class="flex justify-center items-center gap-4">
                                    <div class="rounded-full border border-2 border-blue-500 bg-blue-300 font-semibold text-[#101827]">
                                        <div class="border border-2 border-white p-2 rounded-full">
                                            AE
                                        </div>
                                    </div>
                                    <div>
                                        <div class="font-semibold text-[#101827]">Admin Example</div>
                                        <div class="text-gray-400">
                                            <%= current_user.email %>
                                        </div>
                                    </div>
                                </div>
                                <div>
                                    <div class="text-white px-2 py-1 bg-purple-500 rounded-xl capitalize">
                                        <%= current_user.roles.pluck(:name).join(', ') %>
                                    </div>
                                </div>
                            </div>
                             <div class="p-3 hover:bg-gray-200">
                                <div class="text-gray-500 text-sm">
                                    <%= link_to "Dashboard", root_path %>
                                </div>
                            </div>
                             <div class="p-3 hover:bg-gray-200">
                                <div class="text-gray-500 text-sm">
                                    <%= link_to "Users", users_path %>
                                </div>
                            </div>
                            <div class="border-b p-3 hover:bg-gray-200">
                                <div class="text-gray-500 text-sm">Profile & account</div>
                            </div>
                            <div class="group border-b">
                                <div class="text-gray-500 text-sm">
                                    <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "p-3 hover:bg-red-200 w-full text-left" %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</nav>
<%= javascript_include_tag 'dropdown' %>