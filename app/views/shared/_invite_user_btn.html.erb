<% if current_user.roles.first.name == 'admin' %>
    <a href="<%= invite_users_path %>" class="flex gap-2 items-center mt-3 inline-flex w-fit justify-center rounded-md bg-indigo-600 hover:bg-indigo-500 px-3 py-2 text-sm font-semibold text-white shadow-sm ring-1 ring-inset ring-gray-300 sm:mt-0 sm:w-auto">
        <%= heroicon "user-plus" %> Invite users
    </a>
<% end %>